---
import QualityCareTabs from './preact/composites/QualityCareTabs';

interface Props {
  items: Array<{
    title: string;
    description: string;
    link?: string;
    iconSvg?: string;
  }>;
}

const { items } = Astro.props;
---

<QualityCareTabs client:visible items={items} />
