---
import ServiceLayout from '../../layouts/ServiceLayout.astro';
import ProviderDirectory from '../../components/ProviderDirectory.astro';
import '../../styles/global.css';
---

<ServiceLayout 
  name="Waitlist Times"
  description="Current appointment availability by provider"
>
  <section class="bg-indigo-950 min-h-screen py-16 px-4">
    <div class="max-w-7xl mx-auto">
      <header class="text-center mb-12">
        <h1 class="text-4xl md:text-5xl font-black text-white mb-4">
          Provider <span class="text-amber-500">Availability</span>
        </h1>
        <p class="text-[#D4CFC7] text-lg max-w-2xl mx-auto">
          Filter by service, state, or appointment type to view current wait times.
        </p>
      </header>

      <ProviderDirectory />
    </div>
    <div class="text-left mt-12 text-sm text-[#D4CFC7]">
      <p> * Services excluded by health insurance coverage (self-pay only)</p>
      <p>KY, OH, IN, TN participate in PSYPACT compact. For a list of participating states visit <a href="https://psypact.gov/page/psypactmap" class="text-amber-500 underline">Psypact Map</a>.
      </p>
    </div>
  </section>
</ServiceLayout>